name: FlexDisplay CI

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]
  workflow_dispatch:

jobs:
  rust-host:
    name: Rust Host
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - run: cargo fmt --check --manifest-path host-windows/Cargo.toml
      - run: cargo clippy --manifest-path host-windows/Cargo.toml -- -D warnings
      - run: cargo test --manifest-path host-windows/Cargo.toml
      - run: cargo build --release --manifest-path host-windows/Cargo.toml

  android-client:
    name: Android Client
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle
      - uses: android-actions/setup-android@v3
      - run: chmod +x gradlew
        working-directory: android-client
      - run: ./gradlew assembleDebug --no-daemon
        working-directory: android-client
